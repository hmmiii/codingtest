'''
문제 : 181935 - 홀짝에 따라 다른 값 반환하기
날짜 : 2023-11-03 17:15
## 문제
양의 정수 `n`이 매개변수로 주어질 때, `n`이 홀수라면 `n` 이하의 홀수인 모든 양의 정수의 합을 return 하고 `n`이 짝수라면 `n` 이하의 짝수인 모든 양의 정수의 제곱의 합을 return 하는 solution 함수를 작성해 주세요.

### 제한사항
- 1 ≤ `n` ≤ 100

### 입출력 예
|n|result|
|---|---|
|7|16|
|10|220|
'''

def solution(n):
    answer = 0
    if not(n % 2):
        for i in range(1, n+1):
            answer = answer + i**2 if not(i % 2) else answer + 0
    else:
        for i in range(1, n+1):    
            answer = answer + i if i % 2 else answer + 0
    return answer

'''
## 더 좋아 보이는 다른 사람 풀이

```python
def solution(n):
    return sum(x ** (2 - x % 2) for x in range(n + 1) if n % 2 == x % 2)
```

주어진 함수인 `solution(n)`은 주어진 숫자 `n`에 따라 홀수 또는 짝수 조건에 따라 제곱의 합을 계산하는 함수입니다. 함수의 내용을 해석하겠습니다:

1. `for x in range(n + 1)`: `x`는 0부터 `n`까지의 모든 숫자를 반복합니다.
    
2. `if n % 2 == x % 2`: 주어진 `n`이 홀수이면서 `x`도 홀수인 경우, 또는 `n`이 짝수이면서 `x`도 짝수인 경우에만 아래의 수식을 계산합니다.
    
3. `x ** (2 - x % 2)`: 이 부분은 현재 반복하는 `x` 값을 기반으로 제곱을 계산합니다. `x % 2`는 `x`가 홀수인지 짝수인지 확인하고, `2 - x % 2`는 홀수인 경우 1, 짝수인 경우 2를 반환합니다. 따라서 홀수의 경우 `x`의 1 제곱을, 짝수의 경우 `x`의 2 제곱을 계산합니다.
    
4. `sum(...)`: 조건을 만족하는 `x`에 대한 각 `x ** (2 - x % 2)` 값을 모두 더합니다.
'''